#include "holberton.h"
/**
* int _abs(int) -  is a funcion
* @b: is a variable
* Return: 1 and  + if n is greater than zero, 0 and prints 0 if n is zero, *
*/
int _abs(int b)
{
if (b < 0)
{
return(b * -1);
}	
else
{
return(b);
}
}
