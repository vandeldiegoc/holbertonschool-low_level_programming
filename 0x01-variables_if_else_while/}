#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (success)
 */
int main(void)
{
char ch;
char chx;
{
putchar(ch);
for (ch = 'a'; ch <= 'z'; ch++)
for (chx = 'A'; ch <= 'Z'; ch++)
{
putchar(chx);
}
}
putchar('\n');
return (0);
}
