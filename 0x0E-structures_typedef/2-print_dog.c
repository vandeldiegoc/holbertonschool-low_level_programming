#include "dog.h"
/**
 * init_dog - Short description
 * @name:i First member
 * @age: Second member
 * @owner: Third member
 * @d: Third member
 *
 * Description: Longer description
 */
void print_dog(struct dog *d);
{
if (d == NULL)
return (0);
if ((*d).name == NULL)
printf ("NIL");
if ((*d).age == NULL)	
printf ("NIL");
if ((*d).owner == NULL)	
printf ("NIL");

}
